query GetNodes {
    nodes {
        node_id
        node_type
        battery_level
        last_contact
        neighbours
    }
}

subscription nodeAdded {
    nodeAdded {
        node_id
        node_type
        battery_level
        last_contact
        neighbours
    }
}

subscription nodeRemoved {
    nodeRemoved {
        node_id
        node_type
        battery_level
        last_contact
        neighbours
    }
}

mutation removeNode($id: Int!) {
    removeNode(id: $id)
}